FROM python:3.11-slim

WORKDIR /app

# System deps occasionally required by Pillow / cryptography wheels.
RUN apt-get update \
    && apt-get install -y --no-install-recommends build-essential libffi-dev \
    && rm -rf /var/lib/apt/lists/*

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

# Persistent upload directories (also bind-mounted via docker-compose).
RUN mkdir -p /app/uploads/avatars /app/uploads/media /app/uploads/voice /app/uploads/files

EXPOSE 8001

CMD ["uvicorn", "server:app", "--host", "0.0.0.0", "--port", "8001", "--proxy-headers", "--forwarded-allow-ips", "*"]
